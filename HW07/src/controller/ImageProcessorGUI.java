package controller;

import controller.handler.ImageProcessCommandHandler;

/**
 * This class is a Graphical User Interface for handling commands to process images. It accepts a
 * list of {@link ImageProcessCommandHandler} objects, which are used to delegate properly formatted
 * commands to their relevant handler. To extend the interface, simply add more command objects to
 * its constructor.
 */
public class ImageProcessorGUI {

}
